Our first Database

we can use to create database both upper and lower case.
CREATE DATABASE  db_name;
 => to create database in mysql.

DROP DATABASE db_name;
=> to  delete database in mysql.

USE  db_name;
=> To select the database which you want to work  on it.
=> 

# to run mysql in terminal 
mysql2 -u root -p   then press enter and enter your password of database.

#show all database.
SHOW DATABASES;
#to use database .
USE myapp;

#show table in database.
SHOW TABLES;

#for select all user form table
SELECT * FROM user WHERE username="Arvel12";

